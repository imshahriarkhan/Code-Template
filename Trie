#include<bits/stdc++.h>
using namespace std ;

const int maxN = 5e5 + 5 ;

int next[maxN][27] , sz ;

void insert_string(string &s)
{
    int v = 0 , siz = s.size() ;;
    for(int i = 0 ; i < siz ; ++i)
    {
        int c = s[i]-'a' ;
        if(!next[v][c]) next[v][c] = ++sz ;
        v = next[v][c] ;
    }
}

bool search_prefix(string &tmp)
{
    int v = 0 , siz = tmp.size() ;
    for(int i = 0 ; i < siz ; ++i)
    {
        int c = tmp[i]-'a' ;
        if(!next[v][c]) return false ;
        v = next[v][c] ;
    }
    return true ;
}
